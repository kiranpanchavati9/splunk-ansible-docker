---
- name: Restart Splunk
  community.docker.docker_container:
    name: splunk-cluster-manager
    state: started
    restart: true


- name: Validate Indexer Cluster Bundle
  community.docker.docker_container_exec:
    container: splunk-cluster-manager
    user: splunk
    argv:
      - "{{ splunk_home }}/bin/splunk"
      - validate
      - cluster-bundle
      - -auth
      - "{{ splunk_admin_user }}:{{ splunk_admin_password }}"
  register: cluster_bundle_validation
  changed_when: false
  no_log: false
  listen: Apply Indexer Cluster Bundle


- name: Apply Indexer Cluster Bundle
  community.docker.docker_container_exec:
    container: splunk-cluster-manager
    user: splunk
    argv:
      - "{{ splunk_home }}/bin/splunk"
      - apply
      - cluster-bundle
      - --answer-yes
      - -auth
      - "{{ splunk_admin_user }}:{{ splunk_admin_password }}"
  register: cluster_bundle_apply
  no_log: false
  listen: Apply Indexer Cluster Bundle